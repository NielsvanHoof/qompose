{
  "name": "qompose-platform-infra",
  "private": true,
  "main": "bin/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test bin/*.test.js",
    "preview": "npm run build && pulumi preview",
    "up": "npm run build && pulumi up",
    "destroy": "npm run build && pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^6.76.0",
    "@pulumi/pulumi": "^3.156.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
