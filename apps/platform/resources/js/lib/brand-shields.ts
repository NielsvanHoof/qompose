import shieldLight from '../../images/brand/shield-light.svg';
import shieldPrimary from '../../images/brand/shield-primary.svg';

/** Vite-resolved Qompose shield watermark URLs (works in dev + production). */
export const brandShields = {
    primary: shieldPrimary,
    light: shieldLight,
} as const;
